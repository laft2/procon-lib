# Rules

- **git push の禁止**: `git push` コマンドは実行しないでください。リモートへのプッシュは常にユーザー自身が行います。
- **No git push**: Do not execute `git push` commands. Pushing to remote repositories should always be left to the user.
